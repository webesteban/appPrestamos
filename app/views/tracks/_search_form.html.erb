<%= search_form_for q, url: third_party_types_path, method: :get, html: { data: { turbo_frame: "third_party_types_table" } } do |f| %>
  <div class="row mb-3">
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      <div class="input-group">
        <%= f.search_field :name_cont,
          placeholder: "Buscar tipo tercero...",
          class: "form-control form-control-sm" %>
        <button class="btn btn-outline-secondary btn-sm" type="submit">Buscar</button>
      </div>
    </div>
  </div>
<% end %>